{
  "name": "@walletwall/whale-watcher",
  "version": "1.0.0",
  "description": "Whale Watcher — read-only large-wallet activity lens. Static fixture demo surface. Part of the WalletWall analytics suite.",
  "private": false,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "test:watch": "vitest",
    "security:audit": "npm audit --audit-level=moderate",
    "check": "npm run test && npm run build && npm run security:audit"
  },
  "dependencies": {
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "@testing-library/jest-dom": "^6.6.3",
    "@testing-library/react": "^16.1.0",
    "@testing-library/user-event": "^14.5.2",
    "@vitejs/plugin-react": "^4.6.0",
    "jsdom": "^25.0.1",
    "vite": "^6.3.5",
    "vitest": "^3.2.4"
  }
}
